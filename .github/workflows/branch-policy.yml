---
# Enforces the gitflow model on every pull request.
#
#   feature/*  ->  develop   (all work, including fixes and docs)
#   develop    ->  master    (releases only)
#
# The master ruleset makes this a required check, so a pull request from any
# other branch cannot be merged into master.
name: Branch Policy

on:
  pull_request:
    branches: [develop, master]

permissions: {}

jobs:
  policy:
    name: Branch policy
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: {}
    steps:
      - name: Check source and target branches
        env:
          HEAD: ${{ github.head_ref }}
          BASE: ${{ github.base_ref }}
        run: |
          case "$BASE" in
            master)
              if [ "$HEAD" != "develop" ]; then
                echo "::error::Only develop may be merged into master (got '$HEAD'). Open this pull request against develop."
                exit 1
              fi
              ;;
            develop)
              case "$HEAD" in
                # dependabot/* is the only exception, because Dependabot names
                # its own branches.
                feature/*|dependabot/*) ;;
                *)
                  echo "::error::Branch '$HEAD' does not follow the naming rule. Work happens on feature/<short-description> branches."
                  exit 1
                  ;;
              esac
              ;;
          esac
          echo "$HEAD -> $BASE is allowed."
